# Login-and-Registration-System-
This project is a simple authentication system built using FastAPI, JWT(Json Web Tokens), and MySQL. Here I have not used ORM , instead I'm using sql queries. It allows us to register, login and access the protected routes using token based authentication. This system stores hashed passwords and for connecting to database I have used PyMySQL.
